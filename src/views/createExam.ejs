<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Create Exam - Exam Portal</title>
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <link rel="stylesheet" href="/stylesheets/createExam.css" />
</head>
<body>
  <div class="main-container">
    <header class="header-section">
      <div class="brand-logo">Exam Portal - Admin</div>
      <nav class="navigation-menu">
        <a href="/admin-dashboard" class="nav-link">Dashboard</a>
        <a href="/admin/exams/create" class="nav-link active">Create Exam</a>
        <a href="#" class="nav-link" onclick="showResults()">Results</a>
        <a href="#" class="nav-link" onclick="showSettings()">Settings</a>
      </nav>
      <div class="user-actions">
        <span id="adminName"><%= user ? user.fullname || user.username : 'Admin' %></span>
        <a href="/logout" class="action-btn logout-btn">Logout</a>
      </div>
    </header>

    <main class="content-area">
      <div class="container">
        <div class="page-header">
          <h1>Create New Exam</h1>
          <p class="page-description">Set up a new examination for your organization with coding challenges and detailed configurations.</p>
        </div>
        
        <form id="createExamForm" method="POST" action="/admin/exams">
      <fieldset>
        <legend>Exam Details</legend>
        <div class="inline-grid">
          <label>
            Exam Title *
            <input type="text" name="examTitle" required>
          </label>
          <label>
            Duration (minutes) *
            <input type="number" name="duration" min="1" max="300" required>
          </label>
        </div>

        <div class="inline-grid">
          <label>
            Programming Language *
            <select name="programmingLanguage" required>
              <option value="python">Python 3</option>
              <option value="java">Java</option>
              <option value="cpp">C++</option>
              <option value="c">C</option>
              <option value="javascript">JavaScript (Node.js)</option>
              <option value="go">Go</option>
              <option value="rust">Rust</option>
              <option value="csharp">C#</option>
            </select>
          </label>
          <label>
            Test Case Validation
            <select name="validateTestCases">
              <option value="false">Skip Validation (Recommended)</option>
              <option value="true">Enable Validation (Beta)</option>
            </select>
            <!-- <small style="color: #666; font-size: 0.9rem; display: block; margin-top: 0.5rem;">
              Validation checks test case structure. Enable only if you want to test with Judge0 API.
            </small> -->
          </label>
        </div>
        
        <label>
          Exam Description *
          <textarea name="examDescription" rows="3" required></textarea>
        </label>
        
        <label>
          Instructions
          <textarea name="instructions" rows="4" placeholder="Enter exam instructions for students..."></textarea>
        </label>
        
        <label>
          Start Date & Time *
          <input type="datetime-local" name="startDateTime" required>
        </label>
      </fieldset>

      <fieldset>
        <legend>Questions (1-10)</legend>
        <div id="questionsContainer">
          <!-- Questions will be dynamically added here -->
        </div>
        
        <button type="button" id="addQuestionBtn" class="add-testcase-btn bottom">
          + Add Question
        </button>
      </fieldset>

        <div class="form-actions">
          <button type="submit" class="primary">Create Exam</button>
          <a href="/admin-dashboard" class="secondary">Cancel</a>
        </div>
      </form>
      </div>
    </main>
  </div>

  <script src="/javascripts/sessionProtection.js"></script>
  <script src="/javascripts/createExam.js"></script>
</body>
</html>